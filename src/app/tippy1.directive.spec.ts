import { Tippy1Directive } from './tippy1.directive';

describe('Tippy1Directive', () => {
  it('should create an instance', () => {
    const directive = new Tippy1Directive();
    expect(directive).toBeTruthy();
  });
});
