import { ClickZonelessDirective } from './click-zoneless.directive';

describe('ClickZonelessDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickZonelessDirective();
    expect(directive).toBeTruthy();
  });
});
