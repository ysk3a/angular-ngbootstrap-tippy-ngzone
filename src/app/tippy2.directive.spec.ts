import { Tippy2Directive } from './tippy2.directive';

describe('Tippy2Directive', () => {
  it('should create an instance', () => {
    const directive = new Tippy2Directive();
    expect(directive).toBeTruthy();
  });
});
