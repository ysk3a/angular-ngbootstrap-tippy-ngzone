import { Tippy3Directive } from './tippy3.directive';

describe('Tippy3Directive', () => {
  it('should create an instance', () => {
    const directive = new Tippy3Directive();
    expect(directive).toBeTruthy();
  });
});
